// Arreglos
// Pueden almacenar cualquier tipo de dato de su primera clase, funciones, objetos, numeros, etc
// Tiene longitud
// Cada elemento tiene una posicion  se usa el nomrbe del arreglo mas el indice para acceder a este
// Las posiciones se cuentan desde 0
// La ultima posicion del arreglo es igual a la longitud

let calificacion = [10, 9, 8, 7, 6]

let arreglo = ["rails","laravel","django", 1, {}, function(){}, [1, 23]];

arreglo.pop();

console.log(arreglo);