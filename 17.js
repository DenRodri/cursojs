let arreglo = [1, 2, 3, 4];

for(let i = 0; i < arreglo.length ; i++){
    let elemento = arreglo[i];
    console.log(elemento);
}

// Lo que tienen en común las operaciones forEach, map, reduce, ??
// filter y find, es que son métodos que puedes usar en cualquiera 
// arreglo, y que operan a través de funciones que enviamos como 
// argumento para estos métodos, la sintaxis la iremos destacando en 
// vídeos individuales para cada operación.